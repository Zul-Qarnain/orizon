# Mistral LLM API Key (Optional: deterministic fallback parser is active if key omitted)
MISTRAL_API_KEY=

# Mistral Model Name (default: mistral-small-latest)
MISTRAL_MODEL=mistral-small-latest

# HTTP Server Host and Port
HOST=0.0.0.0
PORT=3000
