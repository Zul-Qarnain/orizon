{
  "name": "gridwise-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "GridWise LLM - Smart Campus Energy Optimization Engine",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run --workspace=@gridwise/shared-types build && npm run --workspace=@gridwise/llm-interpreter build && npm run --workspace=@gridwise/guardrails build && npm run --workspace=@gridwise/optimizer build && npm run --workspace=@gridwise/schedule-validator build && npm run --workspace=@gridwise/api build",
    "vercel-build": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "npm run --workspace=packages/api start",
    "dev": "npm run --workspace=packages/api dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "fastify": "^5.12.5",
    "javascript-lp-solver": "^1.0.3"
  }
}
